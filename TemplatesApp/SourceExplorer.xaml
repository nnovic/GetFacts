<UserControl x:Class="TemplatesApp.SourceExplorer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:wf="clr-namespace:System.Windows.Forms;assembly=System.Windows.Forms"
             xmlns:local="clr-namespace:TemplatesApp"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        
        <!-- url bar -->
        <Grid x:Name="UrlBar" Grid.ColumnSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <ComboBox Grid.ColumnSpan="2"  x:Name="UrlInput" IsEditable="True" Text="https://www.renesas.com/en-eu/about/press-center/news-releases.html"></ComboBox>
            <Button Grid.Column="2" Grid.RowSpan="2" x:Name="BrowseButton" Click="BrowseButton_Click">Browse...</Button>
            <ComboBox Grid.Row="1" SelectedIndex="0" IsEnabled="False">
                <ComboBoxItem>HtmlParser</ComboBoxItem>
            </ComboBox>
            <ComboBox Grid.Row="1" Grid.Column="1" SelectedIndex="0" IsEnabled="False">
                <ComboBoxItem>Autodetect encoding</ComboBoxItem>
            </ComboBox>
        </Grid>
        
        <!-- webbrowser -->
        <Grid Grid.Row="1">
            <FlowDocumentReader x:Name="CodeSourceView"/>
        </Grid>
        
        <!-- tree -->
        <Grid Grid.Column="1" Grid.Row="1">
            <TreeView x:Name="CodeSourceTree" SelectedItemChanged="CodeSourceTree_SelectedItemChanged">
            </TreeView>
        </Grid>
        
    </Grid>
</UserControl>
